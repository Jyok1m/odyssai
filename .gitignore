node_modules/
dist/
packages/db/src/generated/
.next/
.turbo/
out/
build/
*.tsbuildinfo

# Competences d'agents posees par le CLI Prisma, regenerees et symboliquement
# dupliquees en trois exemplaires.
.agents/
**/.claude/skills/
**/.windsurf/skills/
skills-lock.json

.env
.env.*
!.env.example
!.env.local.example

*.log
.DS_Store
.idea

# Chiffrage de la tarification, propre au poste : il porte des hypotheses
# fiscales et une facture de serveur qui n'ont rien a faire dans le depot.
odyssai-tarifs.xlsx

# Mesure du cout reel d'une generation et de quatorze tours. Il consomme des
# appels payants a chaque execution, et son entree n'est donc pas dans le
# package.json : se lance a la main, voir l'en-tete du fichier.
apps/api/scripts/measure-generation.ts

# HOME du conteneur de test de la CI (corepack, cache pnpm).
.ci-home/
